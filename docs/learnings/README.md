# Learnings

Auto-generated engagement learnings and bug reports. ATHENA creates files here during and after engagements to track:

- Engagement session logs (`eng-*.md`)
- Bug reports from beta testing
- Architecture decisions
- Agent behavior observations

**These files are gitignored by default** — they contain engagement-specific data. The README is tracked to preserve the directory structure.
